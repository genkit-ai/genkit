{
  "scripts": {
    "build": "(cd functions && npm i && npm run build) && (npm run build:js)",
    "build:js": "webpack"
  },
  "private": true,
  "dependencies": {
    "firebase": "^12.13.0",
    "jquery": "^4.0.0"
  },
  "devDependencies": {
    "webpack": "^5.90.3",
    "webpack-cli": "^7.0.2"
  }
}
