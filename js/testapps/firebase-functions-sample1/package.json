{
  "scripts": {
    "build": "(cd functions && npm i && npm run build) && (npm run build:js)",
    "build:js": "webpack"
  },
  "private": true,
  "dependencies": {
    "firebase": "^10.14.1",
    "jquery": "^3.7.1"
  },
  "devDependencies": {
    "webpack": "^5.108.3",
    "webpack-cli": "^5.1.4"
  }
}
